import { GridTemplatePipe } from './grid-template.pipe';

describe('GridTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new GridTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
